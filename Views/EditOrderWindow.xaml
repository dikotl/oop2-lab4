<Window
    x:Class="ServiceMarketplace.Views.EditOrderWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Edit Order"
    Height="300"
    Width="300"
    WindowStartupLocation="CenterOwner">

    <Window.Resources>
        <Style TargetType="TextBox">
            <Style.Triggers>
                <Trigger
                    Property="Validation.HasError"
                    Value="True">
                    <Setter
                        Property="BorderBrush"
                        Value="Red"/>
                    <Setter
                        Property="BorderThickness"
                        Value="2"/>
                    <Setter
                        Property="ToolTip"
                        Value="{Binding (Validation.Errors)[0].ErrorContent, RelativeSource={RelativeSource Self}}"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <Style TargetType="ComboBox">
            <Style.Triggers>
                <Trigger
                    Property="Validation.HasError"
                    Value="True">
                    <Setter
                        Property="BorderBrush"
                        Value="Red"/>
                    <Setter
                        Property="BorderThickness"
                        Value="2"/>
                    <Setter
                        Property="ToolTip">
                        <Setter.Value>
                            <Binding
                                Path="(Validation.Errors)/ErrorContent"
                                RelativeSource="{RelativeSource Self}"/>
                        </Setter.Value>
                    </Setter>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>

    <StackPanel Margin="20">
        <TextBlock Text="Select Executor:"/>
        <ComboBox
            ItemsSource="{Binding AvailableExecutors}"
            SelectedItem="{Binding NewOrder.Executor}"
            Margin="0,0,0,10">
        </ComboBox>

        <TextBlock Text="Select Service:"/>
        <ComboBox
            ItemsSource="{Binding AvailableServices}"
            SelectedItem="{Binding NewOrder.Service}"
            Margin="0,0,0,10"/>

        <TextBlock Text="Customer Address:"/>
        <ComboBox
            ItemsSource="{Binding AvailableAddresses}"
            Text="{Binding NewOrder.CustomerAddress, UpdateSourceTrigger=PropertyChanged}"
            IsEditable="True"
            Margin="0,0,0,10"/>

        <TextBlock Text="Cost:"/>
        <TextBox
            Text="{Binding NewOrder.Cost, UpdateSourceTrigger=PropertyChanged}"
            Margin="0,0,0,20"/>

        <StackPanel
            Orientation="Horizontal"
            HorizontalAlignment="Right">
            <Button
                Content="Save"
                Command="{Binding SaveCommand}"
                Width="75"
                Margin="0,0,10,0"/>
            <Button
                Content="Cancel"
                Command="{Binding CancelCommand}"
                Width="75"/>
        </StackPanel>
    </StackPanel>
</Window>
